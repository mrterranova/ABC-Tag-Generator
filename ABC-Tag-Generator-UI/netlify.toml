[build]
  publish = "build"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# netlify.toml
[build.environment]
SECRETS_SCAN_OMIT_KEYS = "REACT_APP_API_URL"